Отличная идея! Вот таблица основных методов и функций **NumPy**, которые часто используются при работе с массивами (`numpy.ndarray`).  

---

## **🔹 Основные методы и функции NumPy (`numpy.ndarray`)**  

### **1. Создание массивов**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `np.array(list)` | Создание массива из списка | `np.array([1, 2, 3])` → `[1 2 3]` |
| `np.zeros(shape)` | Массив из нулей | `np.zeros((2, 3))` → `[[0. 0. 0.], [0. 0. 0.]]` |
| `np.ones(shape)` | Массив из единиц | `np.ones((2, 2))` → `[[1. 1.], [1. 1.]]` |
| `np.empty(shape)` | Массив без инициализации (случайные значения) | `np.empty((2, 2))` → (зависит от памяти) |
| `np.arange(start, stop, step)` | Аналог `range()`, но возвращает массив | `np.arange(0, 5, 1)` → `[0 1 2 3 4]` |
| `np.linspace(start, stop, num)` | Равномерно распределённые числа | `np.linspace(0, 1, 3)` → `[0. 0.5 1.]` |
| `np.random.rand(shape)` | Случайные числа [0, 1) | `np.random.rand(2, 2)` → `[[0.12, 0.95], [0.43, 0.67]]` |
| `np.eye(N)` | Единичная матрица | `np.eye(2)` → `[[1. 0.], [0. 1.]]` |

---

### **2. Атрибуты массивов**  
| Атрибут | Описание | Пример |
|---------|----------|--------|
| `arr.shape` | Размерность массива | `arr = np.array([[1, 2], [3, 4]])` → `arr.shape` → `(2, 2)` |
| `arr.ndim` | Количество осей (размерностей) | `arr.ndim` → `2` |
| `arr.size` | Общее количество элементов | `arr.size` → `4` |
| `arr.dtype` | Тип данных массива | `arr.dtype` → `int64` |

---

### **3. Операции с массивами**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `arr.reshape(shape)` | Изменение формы массива | `np.arange(6).reshape(2, 3)` → `[[0 1 2], [3 4 5]]` |
| `arr.flatten()` | Преобразование в 1D массив | `arr.flatten()` → `[0 1 2 3 4 5]` |
| `arr.T` / `arr.transpose()` | Транспонирование | `arr.T` → `[[0 3], [1 4], [2 5]]` |
| `np.concatenate((a, b), axis)` | Объединение массивов | `np.concatenate((a, b), axis=0)` |
| `np.split(arr, indices)` | Разделение массива | `np.split(arr, [2])` → `[arr[:2], arr[2:]]` |
| `arr.copy()` | Глубокая копия | `arr2 = arr.copy()` |

---

### **4. Математические операции**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `np.sum(arr)` / `arr.sum()` | Сумма элементов | `arr.sum()` → `15` (для `[0 1 2 3 4 5]`) |
| `np.mean(arr)` | Среднее значение | `arr.mean()` → `2.5` |
| `np.min(arr)` / `arr.min()` | Минимальное значение | `arr.min()` → `0` |
| `np.max(arr)` / `arr.max()` | Максимальное значение | `arr.max()` → `5` |
| `np.std(arr)` | Стандартное отклонение | `np.std([1, 2, 3])` → `0.816` |
| `np.var(arr)` | Дисперсия | `np.var([1, 2, 3])` → `0.666` |
| `np.sin(arr)`, `np.cos(arr)` | Тригонометрические функции | `np.sin(np.pi/2)` → `1.0` |
| `np.exp(arr)` | Экспонента | `np.exp(1)` → `2.718` |
| `np.log(arr)` | Натуральный логарифм | `np.log(10)` → `2.302` |

---

### **5. Логические и индексные операции**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `arr[condition]` | Булева индексация | `arr[arr > 2]` → `[3 4 5]` |
| `np.where(condition, x, y)` | Аналог `if-else` | `np.where(arr > 2, 1, 0)` → `[0 0 0 1 1 1]` |
| `np.any(condition)` | Есть ли `True`? | `np.any(arr > 2)` → `True` |
| `np.all(condition)` | Все ли `True`? | `np.all(arr > 2)` → `False` |
| `arr.argsort()` | Индексы для сортировки | `np.array([3, 1, 2]).argsort()` → `[1 2 0]` |

---

### **6. Линейная алгебра (`numpy.linalg`)**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `np.dot(a, b)` / `a @ b` | Умножение матриц | `np.dot(A, B)` |
| `np.linalg.inv(A)` | Обратная матрица | `np.linalg.inv(A)` |
| `np.linalg.det(A)` | Определитель матрицы | `np.linalg.det(A)` |
| `np.linalg.eig(A)` | Собственные значения и векторы | `eigvals, eigvecs = np.linalg.eig(A)` |

---

### **7. Работа с файлами**  
| Метод/Функция | Описание | Пример |
|--------------|----------|--------|
| `np.save('file.npy', arr)` | Сохранение массива в `.npy` | `np.save('data.npy', arr)` |
| `np.load('file.npy')` | Загрузка массива | `arr = np.load('data.npy')` |
| `np.savetxt('file.txt', arr)` | Сохранение в текстовый файл | `np.savetxt('data.txt', arr)` |
| `np.loadtxt('file.txt')` | Загрузка из текстового файла | `arr = np.loadtxt('data.txt')` |

---

## **🔹 Итог**  
NumPy предоставляет мощные инструменты для работы с многомерными массивами, включая:  
✅ **Создание массивов** (`zeros`, `ones`, `arange`, `random`)  
✅ **Операции с формами** (`reshape`, `transpose`, `concatenate`)  
✅ **Математические вычисления** (`sum`, `mean`, `sin`, `exp`)  
✅ **Линейную алгебру** (`dot`, `inv`, `det`, `eig`)  
✅ **Работу с файлами** (`save`, `load`, `savetxt`)  

Если нужно что-то уточнить или добавить — спрашивай! 😊